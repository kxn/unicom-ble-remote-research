# 研究范围

- 本仓库只研究本次实测的联通 BLE 语音遥控器，软件标识 `XFRSD0D_U_R01_846_40091`。
- 本地工作目录为 `E:/temp/unicom-ble-remote-research`。CH582F / 小米产品项目位于 `E:/temp/ch582f`，是独立仓库，不在那里继续本研究，也不把其固件、RBP/3 或发布流程带入这里。
- 区分实测事实、待验证假设和失败候选。生成 WAV 或收到 ATT Write Response 不等于音频解码/业务命令成功。
- 原始证据保留不变。派生文件、配对密钥、运行环境、第三方下载放 `.local/` 或 `.venv/`，不要提交密钥。
- 默认只读取、订阅。写命令必须有明确的实验标签、目标、原始载荷和停止条件。不要对 FD00 等未知服务进行无范围限制的命令遍历。
- 用户暂不测红外。TV 键未观察到 HID 输出，不能写成已确认 IR-only。
- 现有机顶盒没有蓝牙，无法提供原主机握手抓包。
